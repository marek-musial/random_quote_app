import 'package:flutter/material.dart';
import 'package:random_quote_app/core/screen_sizes.dart';

final BoxShadow black = BoxShadow(
  color: Colors.black,
  offset: Offset.zero,
  blurRadius: screenHeight / 80,
);
final BoxShadow white = BoxShadow(
  color: Colors.white,
  offset: Offset.zero,
  blurRadius: screenHeight / 80,
);
